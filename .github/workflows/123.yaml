name: FOFA IPTV Extractor

on:
  schedule:
    - cron: '0 21 * * *'  # UTC时间21点（北京时间次日5点）
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: pip install requests eventlet
          
      - name: Run FOFA extractor
        run: python fofa_extractor.py
        env:
          # 可以在这里设置环境变量覆盖配置
          SELECTED_PROVINCES: "hejiang,guangdong,shanghai"
          SOURCE_TYPES: "hotel,multicast"
          
      - name: Check results
        run: |
          echo "生成的文件:"
          ls -la *.txt *.m3u 2>/dev/null || echo "未找到文件"
          if [ -f "itvlist.txt" ]; then
            echo "频道统计:"
            grep -c ",http" itvlist.txt | xargs echo "总频道数:"
          fi
          
      - name: Commit changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add *.txt *.m3u
          if ! git diff --staged --quiet; then
            git commit -m "🤖 FOFA源更新: $(date +'%Y-%m-%d %H:%M')"
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
